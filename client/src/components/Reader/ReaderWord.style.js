import styled from 'styled-components';

const ReaderWord = styled.span`
  cursor: pointer;
  position: relative;
  border-radius: 4px;

  &:hover {
    background: #a6f7ff;
  }
`;

export default ReaderWord;
